import { Layout as FullLayout, PlotData } from 'plotly.js';
export declare type Plot = Partial<PlotData>;
export declare type Layout = Partial<FullLayout>;
export interface IPlot {
    data: Plot[];
    layout?: Layout;
}
